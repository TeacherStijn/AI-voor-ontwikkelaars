# Opvragen wat er klaar staat voor push?
git status

# Specifiek iets toevoegen?
git add src/Program.cs

# Commit met notitie:
git commit -m "Fix: null-check toegevoegd"

# Alles in huidige map (nieuw/gewijzigd/verwijderd):
git add .
git commit -m "Feature: zoeken toegevoegd"
